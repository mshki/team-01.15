<link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Nunito:wght@400;600;700;800&display=swap" />

<div class="font-[Nunito,sans-serif] flex flex-col gap-5">

  <%# ── Header card ── %>
  <section class="p-6 border-2 border-lime-200 rounded-2xl bg-gradient-to-br from-lime-50 to-green-100 shadow-lg overflow-hidden">
    <div class="flex items-end justify-between gap-4">
      <div>
        <h1 class="text-3xl font-extrabold text-green-900 mb-1">Create New Event 🐸</h1>
        <p class="text-green-900 text-sm">Fill in the details below to create your event.</p>
      </div>
      <img src="/keropi2.png" alt="Keroppi"
        class="w-28 md:w-40 shrink-0 drop-shadow-md self-end hidden sm:block" />
    </div>
  </section>

  <div id="create-event-form-wrapper">
    <%- include("./partials/create-form", {
      pageError: typeof pageError !== 'undefined' ? pageError : null,
      formValues: typeof formValues !== 'undefined' ? formValues : null
    }) %>
  </div>

</div>
